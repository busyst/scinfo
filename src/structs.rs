/*
pub struct Region{
    id: String,
    name: String,
}

impl Region {
    pub fn new(id: String, name: String) -> Self {
        Self { id, name }
    }
    
    pub fn id(&self) -> &str {
        &self.id
    }
    
    pub fn name(&self) -> &str {
        &self.name
    }
}
pub struct ClanInfo{
    id: String,
    name: String,
    tag: String,
    level: i32,
    level_points: i32,
    registration_date: u64,
    alliance: String,
    description: String,
    leader: String,
    member_count: i32,
}

impl ClanInfo {
    pub fn new(id: String, name: String, tag: String, level: i32, level_points: i32, registration_date: u64, alliance: String, description: String, leader: String, member_count: i32) -> Self {
        Self { id, name, tag, level, level_points, registration_date, alliance, description, leader, member_count }
    }
}
pub struct ClanMember{
    name: String,
    rank: ClanRank,
    join_date: u64,
}
pub enum ClanRank{
    RECRUIT,
    COMMONER,
    SOLDIER,
    SERGEANT,
    OFFICER,
    COLONEL,
    LEADER,
}


pub struct Price{
    amount: i32,
    price: i64,
    time: u64,
    additional: String,
}

impl Price {
    pub fn new(amount: i32, price: i64, time: u64, additional: String) -> Self {
        Self { amount, price, time, additional }
    }
}
*/